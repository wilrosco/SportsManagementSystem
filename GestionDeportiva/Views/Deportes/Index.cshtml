@model IEnumerable<GestionDeportiva.Models.Deporte>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Deportes</h2>

<p>
    @Html.ActionLink("Crear nuevo registro", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DeporteNombre)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DeporteNombre)
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.DeporteID }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.DeporteID }) |
            @Html.ActionLink("Borrar", "Delete", new { id=item.DeporteID })
        </td>
    </tr>
}

</table>
